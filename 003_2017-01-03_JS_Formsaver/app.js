(function(){
    new Formsaver('form');
    /**
     * Examples:
     * var form = new Formsaver('form'):
     * form.loadForm() // loads the form
     * form.clear()    // removes all saved values from localStorage
     */
}());